1) 설계 목표 요약

CorePool 하나에만 실물 RWA/Stable을 보관한다.

가격은 Virtual Reserve AMM으로 자동 형성(외부 가격 입력 없음).

실제 체결은 realRWA 한도(cap) 내에서만.

초과 수요는 ReservationManager로 간다.

Derived Pool은 “UI/Quote 엔드포인트” + Router 호출용 얇은 래퍼로만 둔다.

Router는 모든 경로 스왑의 단일 진입점이며, 상태 기반 서킷브레이커도 여기에 넣는다.

1) 컨트랙트 구성도
핵심(필수)

CoreVirtualReservePool (Core AMM)

LiquidityHubRouter (경로/한도/예약 오케스트레이션)

ReservationManager (대기/취소/클레임 상태 머신)

DerivedSpotPoolFactory + DerivedSpotPool (파생 페어 “창구”)

옵션(해커톤에서 1개만 구현해도 됨)

CircuitBreakerModule (Router 내부 라이브러리/모듈로)

FeeCollector / Treasury (수수료 분배)

1) 모듈별 책임과 핵심 함수
(A) CoreVirtualReservePool (RWA/Stable 단일 풀)
책임

실물 RWA/Stable 보관

Virtual Reserve 기반 가격 산출

실제 스왑 실행(단, RWA 방향 출력은 cap 제한)

Router만 호출하도록 제한(보안/일관성)

상태

IERC20 rwa; IERC20 stable;

uint256 realRwa; uint256 realStable;  // (실제로는 balanceOf로도 가능)

uint256 vRwa; uint256 vStable;        // virtual reserve

uint256 feeBps;

address router;

핵심 인터페이스

quoteExactIn(tokenIn, amountIn) -> (amountOut, maxFillOut, priceImpactBps)

swapExactIn(tokenIn, amountIn, minOut, to) -> (amountOutFilled, amountInUsed, amountOutReserved)

이 함수는 “부분 체결 + 나머지 예약”을 지원해도 되고,

혹은 CorePool은 “체결 가능한 분만 처리”하고, 예약은 Router가 처리해도 됨(MVP는 Router 처리 추천).

가격/체결 로직 (개념)

effective reserves:

ERwa = realRwa + vRwa

ESt = realStable + vStable

예: constant product류로 quote

k = ERwa * ESt

stableIn → rwaOutQuote = ERwa - k/(ESt + stableInMinusFee)

cap

rwaOut = min(rwaOutQuote, realRwa)  // 실물 재고 이상 불가

이벤트

SwapExecuted(user, tokenIn, amountInUsed, tokenOut, amountOutFilled, feePaid)

VirtualReservesUpdated(vRwa, vStable) (MVP에서는 governance/owner만)

LiquidityAdded(provider, rwaIn, stableIn)

LiquidityRemoved(provider, rwaOut, stableOut) (해커톤은 빼도 됨)

해커톤 MVP에선 LP 포지션(유동성 토큰)까지 만들 필요 없이, “공급자 주소들만 deposit/withdraw 가능” 같은 단순모드로 가도 됨.

(B) ReservationManager (대기열/취소/클레임)
책임

“체결 못한 수요”를 기록

취소 시 환불

공급자가 RWA를 풀에 추가하면 claim 가능하도록 처리

토큰화하지 않는 상태 기반(양도 불가 기본)

상태 구조 (MVP 권장: 단순 per-user bucket)

mapping(address => uint256) reservedStable;

“RWA를 못 산 stable 금액”을 보관 (혹은 reservedRwaWanted)

mapping(address => uint256) reservedRwa;

(선택) claim 시 받을 RWA 목표량

mapping(address => uint64) createdAt;

uint64 ttl;

핵심 함수

createReservation(user, stableAmount, rwaTarget) : Router만 호출

cancel(user) : 사용자 호출, stable 환불

claim(user) : 사용자가 호출, 가능한 만큼 RWA 지급

availableToClaim(user) -> (rwaAmount, stableUsed) : 뷰

클레임 기준

가장 단순: claim 시점의 CorePool 스왑 로직으로 stable→RWA를 실행(즉시 체결 가능한 만큼만)

이러면 “대기열 FIFO”가 아니게 되지만, 해커톤 MVP에선 괜찮음.

FIFO까지 하려면 Reservation[] queue + 인덱스가 필요(확장 과제).

이벤트

ReservationCreated(user, stableAmount, rwaTarget)

ReservationCancelled(user, refundedStable)

ReservationClaimed(user, stableUsed, rwaReceived)

네가 언급한 “향후 양도/파생”은 Post-Hackathon 섹션으로 두고, MVP는 양도 불가로 고정하는 게 안전.

(C) LiquidityHubRouter (핵심 오케스트레이터)
책임

모든 스왑의 단일 진입점

DerivedPool(표면) 요청을 받아 실제 경로 실행

“ETH→Stable→RWA” 같은 멀티홉 지원

CorePool cap 적용 후 초과분 Reservation 전환

(옵션) 서킷 브레이커 상태 판단 및 cap 조정

상태

CoreVirtualReservePool core;

ReservationManager reservations;

address stable; address rwa;

mapping(address => bool) supportedBaseTokens; (ETH/WETH 등)

circuit breaker params:

enum State { NORMAL, SOFT, HARD, HALT }

thresholds: backlog growth, utilization, etc.

핵심 함수

swapToRwaExactIn(tokenIn, amountIn, minRwaOut, recipient, allowReserve) -> (filledRwa, reservedStable)

tokenIn이 stable이면 바로 CorePool로

tokenIn이 ETH/WETH면 내부에서 stable로 스왑(해커톤은 Mock DEX or fixed-rate로 대체 가능)

quoteToRwaExactIn(tokenIn, amountIn) -> (rwaOutQuote, rwaOutCap, stableNeeded, willReserve)

cancelReservation() / claimReservation()은 ReservationManager로 위임

멀티홉 구현

해커톤 MVP: tokenIn == stable || tokenIn == weth만 지원

weth->stable는:

외부 DEX(유니스왑v2 라우터) 호출 (가능하면)

데모용 MockSwapAdapter로 대체 (심사 데모 안정적)

이벤트

RoutedSwap(user, tokenIn, amountIn, filledRwa, reservedStable)

CircuitStateChanged(prev, next)

(D) DerivedSpotPool (RWA/ETH 등 “창구”)
책임

사용자에게 “이 페어에서 거래하는 느낌” 제공

실체는 Router를 호출

내부 유동성 없음

상태

address baseToken; (ETH/WETH 등)

LiquidityHubRouter router;

핵심 함수

swapExactIn(amountIn, minOut, to, allowReserve) → router.swapToRwaExactIn(baseToken,...)

quoteExactIn(amountIn) → router.quoteToRwaExactIn(baseToken,...)

Factory

createDerivedPool(baseToken)

getDerivedPool(baseToken)

DerivedPool을 컨트랙트로 만드는 이유는 “파생 풀 무한 생성” 메시지를 온체인에서 증명하기 위해서야.
MVP에선 1개만 만들어도 되고, Factory는 심사 포인트로 매우 좋음.

1) 권한/신뢰 모델 (해커톤용 최소)

CorePool.router = LiquidityHubRouter만 swap 호출 가능 (무단 스왑/취약점 축소)

virtual reserves(vRwa, vStable) 업데이트는:

MVP: owner만 (혹은 멀티시그)

“자동 리밸런싱”은 Post-Hackathon

RWA 공급(유동성 예치)은:

MVP: supplierRole (allowlist)

나중: permissionless + 안전장치

1) 서킷 브레이커(선택) 넣는 위치
오라클 없이도 Router에서 “내부 신호”로 상태 전환 가능.

신호: reservation backlog, realRWA utilization, volume spike

조치:

capMultiplier 감소

HARD/HALT면 swap 제한, cancel/claim만 열기

구현 위치

Router 내부: beforeSwap()에서 updateCircuitState() 호출

1) 데이터 흐름 (핵심 시나리오)
사용자: ETH로 RWA 구매

DerivedPool(RWA/ETH) → Router 호출

Router: ETH→Stable 변환(어댑터)

Router: CorePool quote & cap 계산

Router: 가능한 만큼만 CorePool에서 체결

남은 Stable은 ReservationManager에 예치/기록(allowReserve=true일 때)

유저는 즉시 RWA 수령 + 예약 상태 표시

1) 해커톤 구현 우선순위(현실적인 순서)

CoreVirtualReservePool: quote + swap (cap 포함)

ReservationManager: create/cancel/claim (단순 버전)

Router: stable 직접 스왑 + 예약 연결

DerivedPool: thin wrapper (1개만)

(선택) MockSwapAdapter(WETH→Stable)

(선택) Circuit Breaker

1) “코드 스켈레톤”까지 바로 뽑아줄까?
원하면 다음 답변에서 바로:

CoreVirtualReservePool.sol

ReservationManager.sol

LiquidityHubRouter.sol

DerivedSpotPool.sol + Factory

이렇게 Foundry 기준으로 컴파일되는 최소 구현 스켈레톤을 만들어줄게.
딱 하나만 물을게(확인용이 아니라 분기용이야, 대답 없어도 내가 기본값으로 진행 가능):

RWA/Stable은 ERC20 둘 다로 가정해도 돼? (ETH는 WETH로 래핑해서)
